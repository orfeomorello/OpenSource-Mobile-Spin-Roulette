{
  "$schemaComment": "MobileSpinRoulette — Player controls and landing presentation.",
  "schemaVersion": 4,
  "keyboard": {
    "exit": "Escape",
    "mute": "KeyM"
  },
  "homePresentation": {
    "comment": "Allowed home/settings background effects. The selection is stored in local settings.",
    "backgroundAnimationDefault": "none",
    "backgroundAnimationOptions": [
      "none",
      "recommended",
      "c64_parallax"
    ],
    "optionNotes": {
      "none": "Static square grid, no motion (default).",
      "recommended": "Full home atmosphere: drifting grid, scanline, glow, pixel dust and entrance motion.",
      "c64_parallax": "80s multi-plane parallax: starfield layers, pixel hills and ground strip."
    }
  }
}
