{
  "name": "mobilespinroulette",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:audio": "node scripts/prepare-audio.mjs",
    "predev": "npm run prepare:audio",
    "dev": "vite",
    "prebuild": "npm run prepare:audio",
    "build": "tsc -b && vite build && node scripts/stage-worker.mjs",
    "preview": "vite preview",
    "test": "node --experimental-strip-types src/core/game.test.ts && node --experimental-strip-types src/spin/spinEngine.test.ts && node --experimental-strip-types src/wheel/canvasWheel.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.1"
  }
}
